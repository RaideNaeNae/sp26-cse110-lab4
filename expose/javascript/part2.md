1. 3 because i is declared using var in line 6 and changed from the loop.
2. 150 because discountedPrice is delcared using var and is most recently changed to 150.
3. 150 because discountedPrice is delcared using var in line 4 and is most recently changed to 150.
4. This function will return [50, 100, 150] because it iterates through the loop and halves the value of each number in the original array.
5. The code will cause a reference error because i is defined inside the for loop but is called outside of it and therefore does not exist when it is called.
6. The code will cause a reference error because discountedPrice is defined using let which means it doesn't exist outside of the for loop where it is called.
7. 150 will be printed because that is the most recent value of finalPrice which was declared in the same scope as line 14
8. [50, 100, 150] because the loop successfuly goes through all input values and halves them.
9. The code will cause a reference error at line 11 because i was declared within the scope of the for loop and line 11 is outside that scope.
10. 3 will be printed by line 12 because that is the value of teh variable length and the actual length of prices.
11. [50, 100, 150] because while const was used to declare discountedPrice the array, ou can still edit the contents of the array.
12. A. student.name\
    B. student['Grad Year']\
    C. student.greeting()\
    D. student['Favorite Teacher'].name\
    E. student.courseLoad[0]\
13. A. '32' because one operand is a string, string concatenation is performed.\
    B. 1 because subtraction does not work with strings so numbers are used instead.\
    C. 3 because null is considered zero in addition.\
    D. 3null because null is treated as a string for string concatentation.\
    E. 4 because true as a boolean value is equal to 1 as a numeric value.\
    F. 0 because false is 0 and null is 0 as numeric values.\
    G. 3undefined because undefined is treated as a string.\
    H. NaN because when undefined is converted to a numeric value it becomes Nan and all math involving Nan equals NaN.
14. A. true because '2' counts as 2 which is greater than 1.\
    B. false because '2' is after '12' alphabetically.\
    C. true because '2' counts as 2 which is equal to 2.\
    D. false because === checks for value and type and the twos are different types.\
    E. false because true counts as 1 which does not equal 2.\
    F. true because Boolean(2) is both a boolean and a true value.
15. The == operator converts the two operands to the same type before comparing while the === operator does no conversions and checks both value and type.
16. part2-quesion16
17. Result: [2, 4, 6] The array [1, 2, 3] is inputted and doSomething is run on every value which doubles each one.
18. part2-question18
19. 1\
    2\
    3\
    4